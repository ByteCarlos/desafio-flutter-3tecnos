import 'package:flutter/material.dart';

class AppColors {
  // Cores principais
  static const Color primary = Color(0xFFEA1D2C);   // Vermelho 3Tecnos
  static const Color secondary = Color(0xFF4CAF50); // Verde
  static const Color tertiary = Color(0xFF2196F3);  // Azul

  // Cores extras do logo
  static const Color yellow = Color(0xFFFFB300);   // Amarelo
  static const Color dark = Color(0xFF1A1A1A);      // Preto do logo

  // Neutras
  static const Color white = Color(0xFFFFFFFF);
  static const Color light = Color(0xFFF5F5F5);
  static const Color grey = Color(0xFF9E9E9E);
  static const Color darkGrey = Color(0xFF424242);

  // Estados
  static const Color success = Color(0xFF43A047);
  static const Color warning = Color(0xFFFFC107);
  static const Color error = Color(0xFFD32F2F);
  static const Color info = Color(0xFF0288D1);
}
